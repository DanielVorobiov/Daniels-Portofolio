#!/bin/bash

pipeline{
    agent any
    options {
        timestamps()
    }
    stages {
        stage('build'){
            steps{
                
                sh 'cd Portofolio && ls'
                //python manage.py test Portofolio.tests   
                
            }
        }
    }
}
