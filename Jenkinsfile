pipeline{
    agent any
    options {
        timestamps()
    }
    stages{ 
        stage('build') {
            steps{
                stage{
                    ls}
            }
        }
    }
}

