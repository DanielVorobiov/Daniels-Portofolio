#!/usr/bin/perl
pipeline{
    agent any
    options {
        timestamps()
    }
    stages{ 
        stage('build') {
            steps{
                node {
    sh 'echo hello world'
}}
        }
    }
}

