pipeline{
    agent any
    options {
        timestamps()
    }
    stages{
        stage('Run Shell Script on Special Node') {
            steps {
                script {
                    sh "ls -l"
                    }
    }            }
            }
}
